---
description: Run game locally
agent: build
---

Run the game:

```
cargo run --package tabletop $ARGUMENTS
```

Pass `-- --gm` for game master mode, or specify signaling server with `--signaling <ip>:3536`.
