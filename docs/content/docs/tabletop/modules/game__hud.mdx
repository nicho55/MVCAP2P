---
title: "hud"
---

# `hud`

**Path**: `src/game/hud.rs`

## Components (Bevy)

### `HudRoot`

### `RosterPanel`

### `RosterRow`

### `StatusLabel`

### `HintLabel`

### `BackBtn`

### `ScaleUpBtn`

### `ScaleDownBtn`

### `AssignTokenBtn`

## Enums

### `ToolBtn`

**Derives**: Component, Clone, Copy, PartialEq

| Variante | Campos |
|----------|--------|
| `Tool` | `ActiveTool` |
| `Grid` | `GridKind` |
| `CellDelta` | `f32` |
| `Drop` | `DropMode` |

## Funções

### `sz`

```rust
fn sz(n : f32, si : & ScreenInfo) -> f32
```

### `toolbar_clicks`

```rust
fn toolbar_clicks(q : Query < (& Interaction , & ToolBtn) , Changed < Interaction > >, mut tool : ResMut < ActiveTool >, mut drop_mode : ResMut < DropMode >, mut grid : ResMut < GridRes >, mut net : ResMut < Net >, session : Res < Session >) -> ()
```

### `toolbar_visuals`

```rust
fn toolbar_visuals(mut q : Query < (& ToolBtn , & mut BorderColor , & mut BackgroundColor) >, tool : Res < ActiveTool >, drop_mode : Res < DropMode >, grid : Res < GridRes >) -> ()
```

### `status_label`

```rust
fn status_label(net : Res < Net >, session : Res < Session >, roster : Res < Roster >, mut q : Query < (& mut Text , & mut TextColor) , With < StatusLabel > >) -> ()
```

### `back_btn_click`

```rust
fn back_btn_click(q : Query < & Interaction , (Changed < Interaction > , With < BackBtn >) >, mut next : ResMut < NextState < AppState > >) -> ()
```

### `assign_token_click`

```rust
fn assign_token_click(q : Query < (& Interaction , & AssignTokenBtn) , Changed < Interaction > >, selection : Res < TokenSelection >, session : Res < Session >, mut net : ResMut < Net >, roster : Res < Roster >, mut ctx : Ctx3d, mut q_tokens : Query < (Entity , & mut Token , & Children) >, mut q_rings : Query < & mut MeshMaterial3d < StandardMaterial > , With < OwnerRing > >) -> ()
```

### `hint_label`

```rust
fn hint_label(drop_mode : Res < DropMode >, tool : Res < ActiveTool >, assets : Res < GameAssets >, mut q : Query < & mut Text , With < HintLabel > >) -> ()
```

## Systems (Bevy)

### `tool_button`

**Parâmetros**: `bar : & mut ChildSpawnerCommands`, `kind : ToolBtn`, `icon : Handle < Image >`, `si : & ScreenInfo`

### `spawn_hud`

**Parâmetros**: `commands : & mut Commands`, `assets : & GameAssets`, `session : & Session`, `si : & ScreenInfo`

### `setup_hud`

**Parâmetros**: `mut commands : Commands`, `assets : Res < GameAssets >`, `session : Res < Session >`, `si : Res < ScreenInfo >`

### `scale_btn_click`

**Parâmetros**: `q_down : Query < & Interaction , (Changed < Interaction > , With < ScaleDownBtn >) >`, `q_up : Query < & Interaction , (Changed < Interaction > , With < ScaleUpBtn >) >`, `mut si : ResMut < ScreenInfo >`, `mut commands : Commands`, `q_old_hud : Query < Entity , With < HudRoot > >`, `assets : Res < GameAssets >`, `session : Res < Session >`

### `roster_panel`

**Parâmetros**: `mut commands : Commands`, `roster : Res < Roster >`, `session : Res < Session >`, `selection : Res < TokenSelection >`, `assets : Res < GameAssets >`, `si : Res < ScreenInfo >`, `q_panel : Query < Entity , With < RosterPanel > >`, `q_rows : Query < Entity , With < RosterRow > >`

## Constantes

| Nome | Tipo | Valor |
|------|------|-------|
| `GOLD` | `const GOLD : Color = Color :: srgb (0.83 , 0.69 , 0.22) ; . ty` | `const GOLD : Color = Color :: srgb (0.83 , 0.69 , 0.22) ; . expr` |
| `TEXT` | `const TEXT : Color = Color :: srgb (0.92 , 0.90 , 0.95) ; . ty` | `const TEXT : Color = Color :: srgb (0.92 , 0.90 , 0.95) ; . expr` |
| `PANEL` | `const PANEL : Color = Color :: srgba (0.10 , 0.09 , 0.14 , 0.94) ; . ty` | `const PANEL : Color = Color :: srgba (0.10 , 0.09 , 0.14 , 0.94) ; . expr` |
| `PANEL_SOFT` | `const PANEL_SOFT : Color = Color :: srgba (0.10 , 0.09 , 0.14 , 0.75) ; . ty` | `const PANEL_SOFT : Color = Color :: srgba (0.10 , 0.09 , 0.14 , 0.75) ; . expr` |
| `BTN_BG` | `const BTN_BG : Color = Color :: srgb (0.16 , 0.14 , 0.21) ; . ty` | `const BTN_BG : Color = Color :: srgb (0.16 , 0.14 , 0.21) ; . expr` |
| `BTN_BORDER` | `const BTN_BORDER : Color = Color :: srgb (0.30 , 0.26 , 0.40) ; . ty` | `const BTN_BORDER : Color = Color :: srgb (0.30 , 0.26 , 0.40) ; . expr` |
| `SCALE_MIN` | `const SCALE_MIN : f32 = 0.35 ; . ty` | `const SCALE_MIN : f32 = 0.35 ; . expr` |
| `SCALE_MAX` | `const SCALE_MAX : f32 = 2.5 ; . ty` | `const SCALE_MAX : f32 = 2.5 ; . expr` |
| `SCALE_STEP` | `const SCALE_STEP : f32 = 0.1 ; . ty` | `const SCALE_STEP : f32 = 0.1 ; . expr` |

